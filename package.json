{
  "private": true,
  "name": "electron-sign-applescript",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ electron-sign-applescript --platform=mas --arch=x64 --overwrite --osx-sign"
  },
  "devDependencies": {
    "electron": "^1.3.4",
    "electron-packager": "^8.0.0",
    "electron-prebuilt": "^1.3.5"
  },
  "dependencies": {
    "applescript": "^1.0.0"
  }
}
